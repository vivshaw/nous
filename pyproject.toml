[project]
name = "loam"
version = "0"
description = "python toolchain for loam"
requires-python = ">=3.11"

[dependency-groups]
dev = [
    "mypy>=1.13",
    "playwright>=1.62.0",
    "pytest>=8.3",
    "ruff>=0.8",
]

[tool.uv]
package = false

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "UP",  # pyupgrade
    "SIM", # flake8-simplify
]

[tool.mypy]
python_version = "3.11"
files = ["skills/visualizing-data/scripts", "tools"]
warn_redundant_casts = true
warn_unused_ignores = true
warn_unreachable = true
check_untyped_defs = true
exclude = [
    "skills/.*/test_.*\\.py$",
    "tools/(?:.*/)?test_.*\\.py$",
]

[tool.pytest.ini_options]
testpaths = [
    "skills/visualizing-data/scripts",
    "tools",
]
